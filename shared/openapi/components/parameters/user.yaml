UserIDParameter:
  name: user_id
  in: path
  description: The ID of the user.
  required: true
  schema:
    type: string
    format: uuid

UserEmailRegexParameter:
  name: emailRegex
  in: path
  description: The email regex of the user.
  required: true
  schema:
    type: string