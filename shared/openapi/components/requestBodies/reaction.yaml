AddReactionRequest:
  description: Request body for adding a reaction to a message.
  required: true
  content:
    application/json:
      schema:
        type: object
        required: [ emoji ]
        properties:
        emoji:
          type: string
          description: The emoji to add as a reaction. This should be a valid Unicode emoji.