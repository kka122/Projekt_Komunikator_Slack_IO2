DirectChatListResponse:
  description: A list of direct chats.
  content:
    application/json:
      schema:
        type: object
        properties:
          directChats:
            type: array
            items:
              $ref: '../schemas/directChat.yaml#/DirectChat'