CreatePaymentResponse:
  description: Workspace created successfully
  content:
    application/json:
      schema:
        type: object
        required: [ clientSecret ]
        properties:
          clientSecret:
            type: string
            description: Secret key generated by Stripe for processing the payment