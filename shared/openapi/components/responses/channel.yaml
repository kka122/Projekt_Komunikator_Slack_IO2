ChannelListResponse:
  description: A list of channels.
  content:
    application/json:
      schema:
        type: object
        properties:
          channels:
            type: array
            items:
              $ref: '../schemas/channel.yaml#/Channel'